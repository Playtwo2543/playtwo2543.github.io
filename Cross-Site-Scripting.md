[<<back to Main](README.md)

# Cross-Site Scripting
    วิธีการแก้ไขปัญหา Session Fixation ที่เช็คผ่าน RIPS (เนื้อหาวันที่ 11 กุมภาพันธ์ 2569)
---

### ปัญหาที่ RIPS พบ
    โจมตีสามารถแทรกโค้ด HTML หรือ JavaScript เข้าไปในหน้าเว็บของเหยื่อได้
    เพราะแอปพลิเคชันนำ “ข้อมูลจากผู้ใช้” ไปแสดงผลโดยไม่ผ่านการป้องกัน
      สิ่งที่อาจเกิดขึ้น เช่น
        ✨ เปลี่ยนหน้าตาเว็บ (defacement)
        ✨ ทำหน้า phishing หลอกลวง
        ✨ ขโมย cookie
        ✨ hijack session ของผู้ใช้  
        หมายเหตุ: (Session Hijacking เป็นเทคนิคการโจมตีทางไซเบอร์ 
                  ที่ผู้ไม่หวังดี ขโมย session ของเหยื่อ
                  แล้วเอาไปใช้ “ปลอมตัวเป็นเหยื่อ” เพื่อเข้าระบบแทน)

  ---
![PIC1](im/IM1.png)

   ---
   ### ข้อมูลทั่วไป
    ชื่อช่องโหว่: Cross-Site Scripting (XSS)
    ประเภทช่องโหว่: Web Application Security
    ระดับความเสี่ยง: สูง (High Risk)
    ไฟล์ที่ได้รับผลกระทบ: index.php (หรือไฟล์ที่มีการใช้ $_GET และ echo โดยตรง)
    เครื่องมือที่ใช้ตรวจสอบ: RIPS Code Analysis Tool
    สภาพแวดล้อม: PHP Web Application
  
  ---
### ปัญหาที่พบ


---
#### หลักการคือ:
    “ห้ามนำข้อมูลจากผู้ใช้ไปแสดงผลตรง ๆ เด็ดขาด”
ต้องทำการ Encode ก่อนแสดงผลเสมอ

#### ตัวอย่างโค้ดที่มีช่องโหว่ (Vulnerable Code)
    print("Hello " . $_GET["name"]);

### Proof of Concept (ตัวอย่างการโจมตี)
    URL ที่ใช้โจมตี:
          /index.php?name=<script>alert(1)</script>
    ผลลัพธ์: โค้ด <script>alert(1)</script> จะถูกทำงานจริงใน Browser
            แสดงว่าช่องโหว่ XSS ถูกใช้โจมตีได้สำเร็จ

---
### วิธีการแก้ไข
   1 : ตรวจสอบรายละเอียดจาก RIPS
![PIC2](im/IM2.png)

  2 : เชื่อมต่อ Server
      
      ใช้โปรแกรม WinSCP และ เข้าไปยังโฟลเดอร์ของไฟล์ที่มีปัญหา
![PIC3](im/IM3.png)
  
  3 : เปิดไฟล์เพื่อแก้ไข
      
      คลิ๊กที่ไฟล์ดังกล่าวและแก้ไข
![PIC4](im/IM4.png)
#### ตัวอย่างโค้ดที่ปลอดภัยกว่า:

    print("Hello " . htmlentities($_GET["name"], ENT_QUOTES, "utf-8"));

##### อธิบายทีละส่วน
    🪶 htmlentities()
        → แปลงอักขระพิเศษเป็น HTML-safe เช่น
            < → &lt;
            > → &gt;

     🪶 ENT_QUOTES
        → ป้องกันการแทรก ' หรือ " เข้า HTML attribute

      🪶 "utf-8"
        → กำหนด charset ให้ตรง ป้องกันการ bypass
---

  4 : แทนที่ไฟล์
      
      หลังจากแก้ไขเรียบร้อย ให้บันทึก และนำไฟล์ที่พึ่งแก้เสร็จ ไปแทนที่กับอันเก่าที่อยู่บน server

![PIC5](im/IM5.png)

---

  5 : ตรวจสอบผลลัพธ์
    เช็คผ่าน RIPS อีกครั้ง  : พบว่าไม่มีปัญหาแล้ว
    
  ![PIC6](im/IM6.png)
  ![PIC7](im/IM7.png)

---

#### สูตรกัน XSS โดยสรุป
    User Input  +  Direct Output  =  XSS ❌
    User Input  +  Encode ก่อน Output  =  Safe ✅

 ## บทสรุป การแก้ไข Cross-Site Scripting ทำได้โดย
    🛡️ ไม่แสดงข้อมูลผู้ใช้โดยตรง
    🛡️ ใช้ htmlentities() ทุกครั้งก่อนแสดงผล
    🛡️ ตรวจสอบ input อย่างเหมาะสม
    🛡️ ทำให้ระบบมีความปลอดภัยและลดความเสี่ยงจากการถูกโจมตีผ่าน XSS ได้อย่างสมบูรณ์
---
