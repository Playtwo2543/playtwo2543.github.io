[<<back to Main](README.md)

# รายงานการแก้ไขช่องโหว่ Cross-Site Scripting (XSS)

ไฟล์ที่ใช้ในการทดสอบ  
> xss_json.php  
> Path: /var/www/html/bWAPP/xss_json.php

---

## 1. ตัวอย่างการทำงานปกติ (Business Function)

ไฟล์ `xss_json.php` เป็นส่วนหนึ่งของระบบ bWAPP  
โดยผู้ใช้งานจะใช้งานผ่านหน้าเว็บ (User Interface)

### ขั้นตอนการทำงานปกติ

1. ผู้ใช้กรอกชื่อภาพยนตร์ลงในช่อง Input บนหน้าเว็บ  
2. กดปุ่ม Submit  
3. ระบบส่งค่าที่กรอกไปยัง backend  
4. ระบบตอบกลับข้อความในรูปแบบ JSON และแสดงผลบนหน้าเว็บ  

ตัวอย่างการใช้งานปกติ

- ผู้ใช้กรอกคำว่า `IRON MAN`
- ระบบตอบกลับ

Yes! We have that movie...


ดังนั้น Business Function ของหน้านี้คือ

👉 “รับค่าชื่อหนังจากผู้ใช้ผ่าน UI แล้วตอบกลับเป็นข้อความในรูปแบบ JSON”

---

## 2. การทดสอบเชิงเทคนิคก่อนแก้ไข (Pre-Test)

เพื่อให้สามารถทดสอบได้อย่างชัดเจน  
จึงเรียกใช้งาน endpoint โดยตรงผ่าน URL

/xss_json.php?title=IRON MAN


ผลลัพธ์ที่ได้

Yes! We have that movie...


แสดงว่าระบบทำงานได้ตาม Business Function ปกติ

---

## 3. ช่องโหว่ที่พบ

จากการตรวจสอบพบว่าไฟล์นี้มีช่องโหว่ประเภท

> Cross-Site Scripting (Reflected XSS via JSON)

![PIC](im/M0.png)

สาเหตุเกิดจากการนำค่าจาก `$_GET["title"]`  
ไปแสดงผลผ่าน `innerHTML` โดยไม่ผ่านการ Encode

### โค้ดที่มีช่องโหว่

```php
$title = $_GET["title"];
$response = $title . "??? Sorry, we don't have that movie :(";
และในฝั่ง JavaScript

document.getElementById("result").innerHTML =
JSONResponse.movies[0].response;
โครงสร้างของช่องโหว่

Source : $_GET["title"]
Sink   : innerHTML
Result : Reflected XSS
4. การทดสอบโจมตีก่อนแก้ไข
ใช้วิธีเดียวกับข้อ 2
แต่เปลี่ยนค่า input เป็น payload

/xss_json.php?title=<script>alert(1)</script>
ผลลัพธ์ที่เกิดขึ้น

Script ถูก execute จริงใน Browser

มีหน้าต่าง alert แสดงขึ้นมา



แสดงว่า

❌ ระบบมีช่องโหว่ XSS จริง

5. ตรวจสอบด้วยเครื่องมือสแกนโค้ด
ทำการสแกนด้วย RIPS Code Analysis Tool


ผลการสแกนพบว่า

มีการใช้ $_GET

มีการแสดงผลโดยไม่ผ่านการป้องกัน

จัดเป็นช่องโหว่ประเภท Reflected XSS

6. แนวทางการแก้ไข
หลักการป้องกัน XSS คือ

ต้อง Encode ข้อมูลจากผู้ใช้ก่อนนำไปแสดงผลทุกครั้ง

ก่อนแก้ไข
$title = $_GET["title"];
$response = $title . "??? Sorry, we don't have that movie :(";


หลังแก้ไข
$title = htmlentities($_GET["title"], ENT_QUOTES, "UTF-8");

$response = $title . "??? Sorry, we don't have that movie :(";

$data = array(
    "movies" => array(
        array("response" => $response)
    )
);

$string = json_encode(
    $data,
    JSON_HEX_TAG | JSON_HEX_APOS | JSON_HEX_QUOT | JSON_HEX_AMP
);
การแก้ไขสำคัญคือ

ใช้ htmlentities() เพื่อ Encode input

ใช้ json_encode() พร้อม option ป้องกัน script injection

ไม่ส่งข้อมูลดิบของผู้ใช้ไปยังฝั่ง client

7. ทดสอบหลังการแก้ไข (Post-Test)
ใช้วิธีเดียวกับก่อนแก้ไข

/xss_json.php?title=<script>alert(1)</script>
ผลลัพธ์ที่ได้

<script>alert(1)</script>??? Sorry, we don't have that movie :(



ผลที่สังเกตได้

ไม่มี alert แสดงขึ้นมา

Script ไม่ถูก execute

แสดงว่า

✅ ช่องโหว่ XSS ถูกปิดเรียบร้อยแล้ว

8. ตรวจสอบ Business Function หลังแก้ไข
ทดสอบการทำงานปกติอีกครั้ง

/xss_json.php?title=THOR
ผลลัพธ์ยังคงเป็น

Yes! We have that movie...
แสดงว่า

ระบบยังทำงานตาม Business Function เดิมได้ครบถ้วน

การแก้ไขไม่กระทบการทำงานของระบบ

บทสรุป
ปัญหา:

พบช่องโหว่ Reflected XSS ในไฟล์ xss_json.php

สาเหตุ:

นำข้อมูลจากผู้ใช้ไปแสดงผลโดยไม่ Encode

แนวทางแก้ไข:

ใช้ htmlentities() กับ input

ใช้ json_encode() แบบปลอดภัย

ผลลัพธ์:

ปิดช่องโหว่ได้สำเร็จ

ระบบยังทำงานตาม Business Function เดิม

ผ่านการตรวจสอบจาก RIPS

สูตรป้องกัน XSS โดยสรุป
User Input + Direct Output = XSS ❌
User Input + Encode ก่อน Output = Safe ✅

---

อันนี้ Flow จะถูกตามหลักที่ครูต้องการแน่นอน:

✔ เริ่มจาก UI  
✔ แยก testing กับ scanning  
✔ ก่อน–หลังใช้วิธีเดียวกัน  
✔ alert() ใช้เป็น payload ไม่ใช่ business output 
